#!/usr/bin/python3
import os
import re
from printOperations import *

printHeader()
printNav()
printSite()
printFooter()

"""uri = os.environ["REQUEST_URI"]
site = re.findall("site=(\w*)", uri, re.ASCII)
if site != []:
    site = site[0]
else:
"""
